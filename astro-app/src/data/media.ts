import manifest from "./media-manifest.json";

export const shortFormVideos = manifest.sections.shortFormVideos.items;
export const ugcVideos = manifest.sections.ugcSection.items;
export const blogImages = manifest.sections.blogPosts.items;
export const socialImages = manifest.sections.socialMediaPosts.items;
