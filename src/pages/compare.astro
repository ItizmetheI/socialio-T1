---
import BaseLayout from '../layouts/BaseLayout.astro';
import CompareContent from '../components/pages/CompareContent';
---

<BaseLayout
  title="Compare Socialio vs. Agencies & In-House"
  description="See how Socialio's productized model stacks up against traditional marketing agencies and building an in-house team."
  path="/compare"
>
  {/* No client: directive -- this component is pure static JSX (no state, no
      motion, icons render as inline SVG server-side), so it ships zero JS. */}
  <CompareContent />
</BaseLayout>
